## 2024-05-23 - Keyboard Shortcuts & Visual Hints
**Learning:** Users can discover shortcuts naturally when visual hints (kbd tags) are integrated directly into the UI controls they click, rather than hiding them in a separate help menu.
**Action:** When adding keyboard shortcuts, always expose them in the UI element they correspond to using `aria-keyshortcuts` and a visual `<kbd>` indicator.
