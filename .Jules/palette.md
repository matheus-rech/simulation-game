## 2026-01-20 - Keyboard Shortcuts for Game Actions
**Learning:** Adding keyboard shortcuts (L, R) for primary game actions significantly improves the "power user" experience and accessibility in simulation contexts.
**Action:** When designing game-like UIs, always map primary actions to single-key shortcuts and expose them via visual hints (e.g., `<kbd>`).
