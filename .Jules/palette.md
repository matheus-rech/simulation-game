## 2024-05-22 - Keyboard Shortcuts in HUD
**Learning:** Adding visual hints (kbd tags) inside buttons for keyboard shortcuts significantly improves discoverability without cluttering the UI. Using `aria-keyshortcuts` ensures screen readers announce them properly.
**Action:** Always pair global keyboard shortcuts with visual indicators on the corresponding UI controls and use proper ARIA attributes.
