## 2024-05-22 - Keyboard Hints in HUD
**Learning:** Embedding keyboard shortcuts (e.g., `<kbd>L</kbd>`) directly inside action buttons improves discoverability and accessibility without cluttering the interface with a separate "Help" modal.
**Action:** Extend the `HUDButton` pattern to support `shortcut` props for all future primary actions.
